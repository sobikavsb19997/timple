#include<stdio.h>
void main()
{
int k,n,sum=0;
printf("enter the total number of integers");
scanf("%d",&n);
printf("enter no of integers to sum");
scanf("%d",&k);
for(i=0;i<=k;i++);
{
sum=sum+i;
}
printf("the sum is=%d",sum);
}
